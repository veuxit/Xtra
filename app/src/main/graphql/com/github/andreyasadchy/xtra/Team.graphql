query Team($name: String!) {
    team(name: $name) {
        bannerURL
        description
        displayName
        logoURL
        members {
            totalCount
        }
        owner {
            displayName
            login
        }
    }
}