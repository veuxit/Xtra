query ($id: ID, $quality: BadgeImageSize) {
    user(id: $id, lookupType: ALL) {
        broadcastBadges {
            imageURL(size: $quality)
            setID
            title
            version
        }
    }
}
