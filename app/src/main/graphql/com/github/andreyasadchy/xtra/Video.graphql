query Video($id: ID) {
    video(id: $id) {
        broadcastType
        createdAt
        game {
            id
            displayName
        }
        lengthSeconds
        owner {
            displayName
            id
            login
            profileImageURL(width: 300)
        }
        previewThumbnailURL
        title
    }
}
