query UserHosting($id: ID, $login: String) {
    user(id: $id, login: $login) {
        hosting {
            displayName
            id
            login
            profileImageURL(width: 300)
        }
    }
}
